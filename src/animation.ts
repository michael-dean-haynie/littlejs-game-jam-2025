/** handles the logic of animating sprites */
export class Animation {
  /** animation frames */
  // private readonly _frames: ReadonlyArray<AnimationFrame>;

  /** the index of the current animation frame */
  // private _currentFrame = 0;

  /** the engine frame at which `_currentFrame` was first rendered */
  // private _engineFrame: number | null = null;

  // constructor(frames: ReadonlyArray<AnimationFrame>) {
  //   this._frames = frames;
  // }

  update(): void {}
}
