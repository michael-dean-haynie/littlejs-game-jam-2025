import { describe, test } from "vitest";

describe("sandbox", () => {
  test("asdf", () => {
    // const foo = undefined as unknown as { bar: number };
    // function myFunction(a: number, b: number, c = foo.bar) {
    //   console.log({ a });
    //   console.log({ b });
    //   console.log({ c });
    // }
    // myFunction(1, 2);
  });
});
